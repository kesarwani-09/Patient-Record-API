Patient Management REST API built with FastAPI and Pydantic for efficient CRUD operations.
Patient data is currently stored in a JSON file, making the project lightweight and easy to understand.
Pydantic models are used for request validation and data consistency.
The project is designed with a structure that can be extended to SQL databases in future versions.
